version: 2.1
workflows:
  test_app:
    jobs:
      - build
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: curl -o- -L https://yarnpkg.com/install.sh | bash
      - run: yarn
      - run: yarn images:pull
